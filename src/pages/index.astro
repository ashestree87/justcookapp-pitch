---
import MainLayout from "../layouts/MainLayout.astro";
import HeroSection from "../components/sections/HeroSection.astro";
import ProblemSection from "../components/sections/ProblemSection.astro";
import SolutionSection from "../components/sections/SolutionSection.astro";
import AppDemoSection from "../components/sections/AppDemoSection.astro";
import MarketSection from "../components/sections/MarketSection.astro";
import DataVizSection from "../components/sections/DataVizSection.astro";
import HowItWorksSection from "../components/sections/HowItWorksSection.astro";
import TechStackSection from "../components/sections/TechStackSection.astro";
import ArchitectureSection from "../components/sections/ArchitectureSection.astro";
import PerformanceSection from "../components/sections/PerformanceSection.astro";
import CodeSnippetsSection from "../components/sections/CodeSnippetsSection.astro";
import TeamSection from "../components/sections/TeamSection.astro";
import ContactSection from "../components/sections/ContactSection.astro";
import PitchDeckModal from "../components/PitchDeckModal.astro";
import DeRiskingSection from "../components/sections/DeRiskingSection.astro";

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<MainLayout title="JustCook.app - Where Cooking Meets Content">
	<HeroSection />
	<ProblemSection />
	<SolutionSection />
	<AppDemoSection />
	<MarketSection />
	<DataVizSection />
	<HowItWorksSection />
	<TechStackSection />
	<ArchitectureSection />
	<PerformanceSection />
	<!-- <CodeSnippetsSection /> -->
	<TeamSection />
	<DeRiskingSection />
	<ContactSection />
</MainLayout>

<PitchDeckModal />

<!-- Floating Financial Model Button -->
<div class="financial-model-fab">
	<a href="/financials" title="View Financial Model">
		📊 Financial Model
	</a>
</div>

<style>
	.financial-model-fab {
		position: fixed;
		bottom: 2rem;
		right: 2rem;
		z-index: 1000;
	}

	.financial-model-fab a {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		background: #2ec4b6;
		color: white;
		padding: 1rem 1.5rem;
		border-radius: 50px;
		text-decoration: none;
		font-weight: 600;
		box-shadow: 0 4px 12px rgba(46, 196, 182, 0.3);
		transition: all 0.3s ease;
		white-space: nowrap;
	}

	.financial-model-fab a:hover {
		background: #26a69a;
		transform: translateY(-2px);
		box-shadow: 0 6px 20px rgba(46, 196, 182, 0.4);
	}

	@media (max-width: 768px) {
		.financial-model-fab {
			bottom: 1rem;
			right: 1rem;
		}
		
		.financial-model-fab a {
			padding: 0.75rem 1rem;
			font-size: 0.9rem;
		}
	}
</style>

<script>
	// Fade-in animation for elements
	document.addEventListener("DOMContentLoaded", () => {
		const observer = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add("visible");
					}
				});
			},
			{ threshold: 0.1 },
		);

		document
			.querySelectorAll(
				".card, .feature-item, .expansion-phase, .team-member",
			)
			.forEach((el) => {
				el.classList.add("fade-in");
				observer.observe(el);
			});
	});
</script>
